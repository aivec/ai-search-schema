# v1.1.1 - Class Prefix Migration

Release date: 2026-01-22

## Overview

This release completes the class prefix migration to `AVC_AIS_*` and aligns filenames with WordPress coding standards.

## Breaking Changes

- Renamed legacy class prefix `AI_Search_Schema_*` → `AVC_AIS_*` (no backward-compat).
- Added explicit public entrypoint `avc_ais_init()` for plugin initialization.

## Build & Maintenance

- Renamed class filenames to match the new prefix for PHPCS compliance.
- Updated translation catalogs and Composer autoload metadata.
